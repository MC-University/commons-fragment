#%RAML 1.0 ResourceType
usage: Retrieve list of all records and/or create a single record

get:
  displayName: Get Resource Type for <<resourcePathName | !singularize | !uppercamelcase>>
  description: Get Resource Type for <<resourcePathName | !singularize | !uppercamelcase>>
  responses:
    200:
      description: |
        Success Response
      body:
        application/json:
          example: <<getResponseExample>>
          type: <<getResponseType>>
    500:
      description: |
        Internal Server Error
      body:
        application/json:
          example: <<errorResponseExample>>
          type: <<errorResponseType>>
    502:
      description: |
        Bad Gateway
      body:
        application/json:
          example: <<errorResponseExample>>
          type: <<errorResponseType>>
    504:
      description: |
        Gateway Timeout
      body:
        application/json:
          example: <<errorResponseExample>>
          type: <<errorResponseType>>
post:
  displayName: Post Resource Type for <<resourcePathName | !singularize | !uppercamelcase>>
  description: Post Resource Type for <<resourcePathName | !singularize | !uppercamelcase>>
  responses:
    201:
      description: |
        Success Response
      body:
        application/json:
          example: <<postResponseExample>>
    400:
      description: |
        Bad Request
      body:
        application/json:
          example: <<errorResponseExample>>
          type: <<errorResponseType>>
    500:
      description: |
        Internal Server Error
      body:
        application/json:
          example: <<errorResponseExample>>
          type: <<errorResponseType>>
    502:
      description: |
        Bad Gateway
      body:
        application/json:
          example: <<errorResponseExample>>
          type: <<errorResponseType>>
    504:
      description: |
        Gateway Timeout
      body:
        application/json:
          example: <<errorResponseExample>>
          type: <<errorResponseType>>